[pytest]
markers =
    real_llm: tests that call the configured real LLM provider and require network/API credentials
    full_pipeline: real LLM tests that run the full AutoRealize pipeline end to end
    slow: tests that may take longer than normal unit tests
